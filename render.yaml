services:
  - type: web
    name: rest-assured-test-automation
    env: docker
    region: oregon
    plan: starter
    dockerContext: .
    dockerfilePath: Dockerfile
    healthCheckPath: /api/test-config/health
    envVars:
      - key: SERVER_PORT
        value: 8080
